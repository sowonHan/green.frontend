*** 클라이언트와 서버가 데이터를 주고받기 위한 비동기 HTTP 통신 기술 ***
1. AXIOS : 클라이언트 라이브러리. cnd 필요
2. fetch : 자바스크립트 내장 라이브러리. 익스플로러에서는 지원 안 해서 polypill 같이 쓰면 됨
(babel이 최신 문법을 구형으로 바꿔줘서 괜찮다고는 함)
3. Jquery Ajax


*** async (asynchronous : 비동기) ***
비동기 코드 : 순서대로 실행하지 않고 여러 가지를 동시에 함. 응답이 올 때까지 기다리지 않음
            ex) ajax, setTimeout 함수

동기 코드 : 순서대로 실행. 응답이 올 때까지 기다림